spring:
  application:
    name: @project.artifactId@
  profiles:
    active: @activatedProperties@,main,db,redis
  cloud:
    nacos:
      discovery:
        namespace: @nacosNamespace@
        group: @nacosGroup@
        username: @nacosUsername@
        password: @nacosPassword@
        server-addr: @nacosIpPort@
        weight: @nacosWeight@

