#parse("utils.vm")
package ${basicConfig.packageName}.vo;
import javax.validation.constraints.NotEmpty;
import javax.validation.constraints.Pattern;
import java.util.*;
/**
*  ${dbTableConfig.multiTableDesc}实体类
*
* @author ${basicConfig.author}
* @since ${basicConfig.dataTime}
*/
@Data
@ApiModel("${dbTableConfig.multiTableDesc}")
public class ${dbTableConfig.multiTableNameCase}Query  {
#foreach( $qItem in $queryConfig)
@ApiModelProperty(value = "$qItem.desc")
    #getValid($qItem)
private ${qItem.type} $qItem.frontField;
#end
}
